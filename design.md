# 落雨小幫手 · 功能改善設計文件 (design.md)

## 1. 改善背景與目的

本文件記錄「落雨小幫手 (watch-ncdr-tool)」近期針對 UI 互動與狀態連動問題的修正與功能升級規劃：

1. **修正村里選單 Reset 機制連動缺失**：解決使用者切換「縣市」時，舊鄉鎮的「村里」下拉選單未連動重置，導致 UI 狀態與圖資不一致的問題。
2. **新增面板收合 / 展開 (Minimize / Expand) 機制**：改善原本點擊 `✕` 直接完全隱藏面板的缺點，提供浮動標題列收合模式，允許使用者隨時收合與展開面板，免去重新點擊書籤的困擾。

---

## 2. 功能一：村里選單 Reset 連動機制修復

### 2.1 目前問題點分析
在 `src/rain.js` 中，`coSel.onchange`（縣市選單變更）觸發時：
- 僅更動了 `rhtw`（鄉鎮選單）的 `<option>` HTML。
- **缺失**：未連動重置 `rhvill`（村里選單），且未清空 `curVill`、`villList`、`villCode` 與 `pendingLL` 內部全域變數。
- **後果**：切換縣市後，村里選單仍顯示上一個縣市鄉鎮的村里選項，地圖紅框與統計資訊也未同步清空。

### 2.2 預期行為與實作規劃
當 `coSel.onchange` 觸發時，必須執行完整的 Cascade Reset（連動重置）：
1. **重置鄉鎮選單 (`rhtw`)**：維持現有邏輯，重建該縣市對應之鄉鎮選項（預設值為 `--`）。
2. **重置村里選單 (`rhvill`)**：
   - 選單內容設為 `<option value="">（先選鄉鎮）</option>`。
   - `disabled` 設為 `false`（或與鄉鎮未選擇狀態保持一致）。
3. **清空村里與鄉鎮全域狀態**：
   - `curVill = null`
   - `villList = []`
   - `villCode = ''`
   - `pendingLL = null`
   - `curIdx = -1`
   - `curKey = ''`
   - `curMk = null`
4. **清理地圖畫面與統計面板**：
   - 呼叫 `clearShapes()` 清除地圖上的鄉鎮/村里紅框外框。
   - 重置 `#rhstatus` 內容為 `"請選擇地區"`。
   - 清空 `#rhsum`、`#rhwarn` 與 `#rhresult` 表格內容。

---

## 3. 功能二：面板收合與展開 (Minimize & Expand) 機制

### 3.1 目前問題點分析
目前點擊標題列右上角的 `✕` 按鈕時，會直接執行：
```javascript
p.style.display = 'none';
clearShapes();
```
這會使整個 UI 面板完全從畫面上消失。使用者若想再次檢視資料，必須重新點擊瀏覽器書籤列上的 Bookmarklet，體驗較不流暢。

### 3.2 預期行為與 UI/UX 設計
在標題列 (`#rhbar`) 加入**收合 (Minimize)** 狀態支援：

1. **標題列控制項調整**：
   - 標題列右上角維持原有的 `✕`（完全關閉），並在旁新增或整合 `—` (收合) 控制項。
   - 或者，在面板縮小/收合時，呈現精巧的浮動膠囊按鈕狀態：
     - **展開狀態 (Expanded)**：顯示完整查詢面板（縣市/鄉鎮/村里選單、圖表、狀態列）。
     - **收合狀態 (Collapsed)**：僅保留頂部標題列 `#rhbar`，隱藏內容區 `#rhbody` (`display: none`)，標題字樣調整為 `🌧️ 落雨小幫手 (已收合 · 點擊展開)`。
2. **互動行為**：
   - 點擊 `—` 按鈕或雙擊標題列：切換面板為「收合」狀態。
   - 處於收合狀態時：
     - 點擊標題列任何位置即可「展開」面板。
     - 地圖紅框暫時隱藏 (`clearShapes()`)。
     - 自動更新驅動器 (DRIVE_TICK) 暫停連網，節省網路與效能開銷。
   - 展開時：
     - 恢復 `#rhbody` 顯示。
     - 自動觸發 `drawShapes()` 重畫目前選定行政區之外框。
     - 觸發畫面/數據復原，維持最新雨量即時資料。

---

## 4. 修改檔案與開發異動步驟

按照專案鐵則（`CLAUDE.md`），後續開發將遵循以下標準流程：

1. **修改源頭**：僅修改 `src/rain.js` 原始碼。
2. **建置編譯**：執行 `npm run build` 產生壓縮產物 `dist/rain.js` 並蓋上建置日期。
3. **文件同步**：
   - 在 `CHANGELOG.md` 最上方新增日期與簡短變更摘要。
   - 更新 `docs/record.md` §7（異動明細）與 §4（現況）。
4. **實機驗證**：於 Chrome 中的 NCDR 官網頁面注入最新版 `dist/rain.js` 驗證功能。

---

## 5. 測試案例 (Test Cases)

| 編號 | 測試項目 | 測試步驟 | 期望結果 |
| --- | --- | --- | --- |
| TC-01 | 縣市切換時村里連動 Reset | 1. 選擇「屏東縣」→「獅子鄉」→「楓林村」<br>2. 將縣市切換為「台北市」 | 鄉鎮選單更新為台北市鄉鎮；村里選單重置為「（先選鄉鎮）」；紅框與舊表格數據完全清除。 |
| TC-02 | 面板收合功能 | 1. 點擊標題列收合按鈕 `—`<br>2. 觀察面板狀態 | 面板主體 `#rhbody` 隱藏，僅保留標題列浮動於右上角，標題提示「點擊展開」，地圖紅框暫時隱藏。 |
| TC-03 | 面板展開與恢復 | 1. 在收合狀態下，點擊標題列 | 面板主體 `#rhbody` 恢復顯示，地圖紅框重新出現，表格資料正常呈現。 |
| TC-04 | 重新選取鄉鎮後的村里重新載入 | 1. 切換縣市後，再選取新鄉鎮（如「台北市」→「士林區」） | 村里選單正確顯示「（載入中…）」並順利載入士林區對應之村里清單。 |
