# 變更紀錄 CHANGELOG

> 給人快速掃視「最近改了什麼」用的精簡清單，**日期式、最新在上、一列一筆**。
> 本工具不再維護版本號——面板／安裝頁顯示的是 build 當天日期（建置識別碼）。
> 每次改完 `src/rain.js` 並 `npm run build` 後，在最上方加一列。
> 完整脈絡、每個 bug 的根因與修法見 [`docs/record.md`](./docs/record.md) §7。

| 日期 | 變更摘要 |
| --- | --- |
| 2026-07-28 | **新增 API 服務**（`npm run api`）：不開瀏覽器、不進官網頁面，直接用縣市+鄉鎮／+村里／經緯度查雨量，回 JSON。工具本體（`src/rain.js`／`dist/rain.js`／`index.html`）一行未動。與面板逐格比對：類型與峰值 16/16 一致、等級 15/16，覆蓋% 因鄉鎮界線來源不同而系統性低 2~3 個百分點（已量化，見 `record.md` §15.10）。順帶修正 `record.md` §10 一條過期敘述（等級取的是主色帶，不是最大值） |
| 2026-07-28 | 建置識別碼改為 `日期.hash4`（hash 取自 `src/rain.js` 內容）——原本只有日期，同一天多次 build 會撞號，導致重入保護把新版當同版本、新程式碼載不進去 |
| 2026-07-28 | 修正切換「縣市」時村里選單與選取狀態沒連動重置（畫面會殘留舊鄉鎮的村里清單、外框與表格），並作廢進行中的查詢；新增面板**收合／展開**（`—` 收成小膠囊、膠囊顯示目前查的地區、收合期間仍持續更新、`✕` 維持完全關閉）。另修掉世代檢查漏在進度回呼的問題（狀態列會卡在「查詢 舊鄉鎮 … 16/16」） |
| 2026-07-28 | 簡化：**移除「範圍」下拉與整組半徑圓功能**。取樣範圍改成單純跟著行政區——有選村里就算該村里，否則算整個鄉鎮。代價是山區大村（獅子鄉內獅村 81.8 km²）失去再細分的能力、村里圖資失敗時只能查整鄉；復原依據保留在 `record.md` §12。dist 23.4→20.1 KB、書籤 37.8→32.4 KB |
| 2026-07-28 | 村里取樣：新增第三層「村里」下拉，可直接以村里邊界統計（實測獅子鄉同一時刻楓林村 100%、南世村 0%）。圖資取自內政部村里界圖開放資料，由 `npm run build:vill` 切成 368 個小檔、經 jsDelivr 按需載入（平均 11 KB），載入失敗只停用村里下拉；並修掉並行查詢競態（名稱與格數會來自不同次查詢） |
| 2026-07-28 | 取樣範圍可縮小：新增「範圍」選單，除整個鄉鎮外可改用 0.5／1／2／5 km 半徑圓（點地圖選圓心），解決大鄉鎮覆蓋% 被稀釋的問題；面板顯示等效雷達格數；順手修掉「按 ✕ 後地圖標示被定時器畫回來」 |
| 2026-07-22 | 去版本號改記錄式：`VER` 改為 build 自動蓋日期、廢除 `npm version` 升版儀式、新增本 CHANGELOG、pre-commit 只擋 dist 過期 |
| 2026-07-22 | 自動更新：表格跟著真實時間前進，不再是選取當下的靜態快照（原 v1.7） |
| 2026-07-18 | 交付結構改版：IIFE → `window.__RH_MAIN`，安裝頁改由 JS 自動產生（原 v1.6） |
| 2026-07-18 | 等級改為雨量雲圖色塊標籤，文字色自動取對比（原 v1.5） |
| 2026-07-18 | 呈現改版：mm 範圍修正為兩帶一階、類型欄、色塊、深色主題（原 v1.4） |
| 2026-07-18 | 準確度：取樣改鄉鎮多邊形遮罩、覆蓋% 不再取整成 0；追加版本感知重入保護（原 v1.3） |
| 2026-07-18 | 效能與體驗：並行載入、雙層快取、摘要列、耗時顯示（原 v1.2） |
| 2026-07-18 | 四項修正：色盤錯配、選點偏移、座標系、重入保護（原 v1.1） |
| — | 初版：下拉選單 + 地圖點選 + 紅框 + 16 格時間表（原 v1.0） |
